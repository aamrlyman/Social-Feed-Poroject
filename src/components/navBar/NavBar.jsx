import "./NavBar.css"

const NavBar = (props) => {

    return (
        <nav className="nav">
          <h1 class="nav">Social<span className='text-muted'>Feed</span></h1>
        </nav>
      );
}
 
export default NavBar;